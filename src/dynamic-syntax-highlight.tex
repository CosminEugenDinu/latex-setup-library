\documentclass[12pt]{article}
\usepackage[a5paper,margin=5mm,bottom=15mm]{geometry}
\usepackage{fontspec}
\setmainfont{Segoe UI}

\begin{document}
\newcommand{\cod}[1]{}

Dynamic syntax highlighting. \\


\newcommand{\code}[2][]{
    % input require compile flag -shell-escape
    \fontspec{Consolas}
    \scriptsize
    \input{|../lib/syntax-highlight/syntax_highlight.py #1 '../src/dynamic-syntax-highlight.tex'}
}

\code['Hello World Program.py']{
re_ends = [re.compile('%s$'%cond) for cond in end_conditions]
}{}

\code[snip.py]{
'single quotes string'
"double quotes string"
'''triple single quotes string'''
"""triple double quotes string""" 
str_len-len(str("some string"))
x = [1,2,3]
2*3-len(x) | str(len(x)) alen(y) len(x) len (y) len(str(s)) 
def func:
    print("ceva")
    print('altceva')

}{}

\end{document}